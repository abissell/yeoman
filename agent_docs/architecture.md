# Architecture

* The `functions` module should be used for standalone pieces of implementation, like "read metadata X from a file"
* The `tools` module should be used for the CLI components that users interface with
